services:
  api:
    build:
      context: ./backend/Api
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ConnectionStrings__Default: Data Source=/app/Data/plato.db
    volumes:
      - plato-data:/app/Data

  frontend:
    build:
      context: ./frontend
    restart: unless-stopped
    depends_on:
      - api
    environment:
      NGINX_PROXY_URL: http://api:8082
    ports:
      - "8081:80"

volumes:
  plato-data:
